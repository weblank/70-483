namespace _3._2._2.Working_with_Encryption_in_the_DotNetFramework
{
    class Program
    {
        static void Main(string[] args)
        {
            // Listing 3.17 - Use a symmetric encryption algorithm
            #region Listing 3.17
            //var listing = new MainListing3X17();
            #endregion

            // Listing 3.18 - Exporting a public key
            // Asymmetric Encryption - Podemos usar as classes RSACryptoServiceProvider
            // e DSACryptoServiceProvider.
            // Geralmente quando usamos criptografia assimétrica usamos a chave pública para
            // encriptar e a chave privada para decriptar.
            #region Listing 3.18
            //var listing = new MainListing3X18();
            #endregion

            // Listing 3.19 - Using a public and private key to encrypt and decrypt data
            #region Listing 3.19
            //var listing = new MainListing3X19();
            #endregion

            // Listing 3.20 - Using a key container for storing an asymmetric Key
            #region Listing 3.20
            //var listing = new MainListing3X20();
            #endregion
        }
    }
}
